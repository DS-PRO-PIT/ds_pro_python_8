name: Воспроизводимость кода

on: workflow_dispatch

jobs:
  reproduce:
    name: Воспроизводимость кода
    runs-on: ubuntu-latest

    steps:
    - name: Reproducing
    - uses: actions/checkout@v1
    - uses: ./reproduce
